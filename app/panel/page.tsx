import Dashboard from "@/components/Dashboard";

export const metadata = { title: "Yönetim Portalı | Teksanor", robots: { index: false, follow: false } };

export default function PanelPage() {
  return <Dashboard />;
}

